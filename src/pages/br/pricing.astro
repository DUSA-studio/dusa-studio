---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PricingContent from '../../components/PricingContent.astro';
import { getRegion, convertPrice } from '../../i18n/regions.js';
import t from '../../i18n/translations/pt.json';

const region = getRegion('br');
const prices = {
  launchpad: convertPrice(47, 'br'),
  starter: convertPrice(149, 'br'),
  growth: convertPrice(297, 'br'),
};
---
<BaseLayout
  title={t.pricing.metaTitle}
  description={t.pricing.metaDescription}
  lang={region.htmlLang}
  ogLocale={region.ogLocale}
  t={t}
>
  <PricingContent region={region} t={t} prices={prices} regionKey="br" />
</BaseLayout>
